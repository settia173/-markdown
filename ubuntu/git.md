ctrl + h 显示隐藏文件夹

git status #查看版本库的状态

git add . #添加当前目录下的所有文件### 添加文件到**stage**中

git commit -m "wrote a test file" //提交

git push -u origin master //将本地内容推送到远程仓库（第一次）

##### git push origin AO

git push origin master //将本地内容推送到远程仓库（之后）

git remote -v //查看远程仓库信息
